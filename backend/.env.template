DATABASE_URL=postgresql://postgres:postgres@localhost:5432/db
# Signals to the backend to turn off CORS
ENVIRONMENT=dev

# --- Garage / S3 object storage ---
# Running the backend on the host (cargo run / cargo run --example garage_smoke),
# Garage is reachable on localhost. Inside docker compose use http://garage:3900.
S3_ENDPOINT=http://localhost:3900
S3_REGION=garage
S3_BUCKET=test-bucket
# Must match the values in the root .env — Garage creates this key
# automatically on first start. Local-dev-only credentials; never reuse
# in production.
AWS_ACCESS_KEY_ID=GK78adb9bda5c63659600fb31e8d3ef4f3
AWS_SECRET_ACCESS_KEY=2584b0fcbfa0c3d4092a0b8363df6b96ae5463d6b1af88563c1fa2eefae67079
