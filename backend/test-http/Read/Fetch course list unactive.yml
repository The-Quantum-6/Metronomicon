info:
  name: Fetch course list unactive
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/courses?status=unactive
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
