info:
  name: List contributions
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/contributions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
