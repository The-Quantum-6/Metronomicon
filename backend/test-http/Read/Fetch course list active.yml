info:
  name: Fetch course list active
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/courses?status=active
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
