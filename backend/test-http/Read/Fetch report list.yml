info:
  name: Fetch report list
  type: http
  seq: 30

http:
  method: GET
  url: http://localhost:3000/reports
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
