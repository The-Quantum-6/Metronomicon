info:
  name: Create project idea
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/project_idea
  body:
    type: json
    data: |-
      {
        "Create": {
          "course_id": "01428fad-79a3-4aee-b97d-05b5fc3b29f6",
          "title": "Multiplayer tic-tac-toe over sockets",
          "body": "Two clients connect to a server and play against each other in real time",
          "difficulty": "Easy"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
